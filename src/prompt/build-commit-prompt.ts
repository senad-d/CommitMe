/**
 * Planned weak-model-friendly Conventional Commit prompt builder.
 *
 * TODO(implementation session): convert structured git/project context into a
 * compact, deterministic prompt that asks for exactly one commit message.
 */
