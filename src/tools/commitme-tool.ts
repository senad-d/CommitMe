import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Planned `commitme` tool registration.
 *
 * TODO(implementation session): add the TypeBox schema, promptSnippet,
 * promptGuidelines, context gathering, truncation, and optional commit action.
 * This placeholder must not register a runtime tool.
 */
export function registerCommitMeTool(_pi: ExtensionAPI) {
  // Preparation-only stub. No runtime behavior is registered here.
}
