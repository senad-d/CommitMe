import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Planned `/commitme` command registration.
 *
 * TODO(implementation session): register the real command after following
 * specs/spec-architecture.md, specs/spec-guidelines.md, and
 * specs/spec-tasks.md. This placeholder must not register a runtime command.
 */
export function registerCommitMeCommand(_pi: ExtensionAPI) {
  // Preparation-only stub. No runtime behavior is registered here.
}
