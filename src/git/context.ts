/**
 * Planned git context collector.
 *
 * TODO(implementation session): gather branch, status, staged and unstaged
 * diffs, changed files, project metadata, safe snippets, and truncation
 * metadata. Keep draft mode read-only.
 */
