import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * CommitMe entry point.
 *
 * Preparation-only placeholder: this package has been renamed and planned, but
 * it intentionally does not register the planned `/commitme` command or the
 * planned `commitme` tool yet. Implement those in a separate session by
 * following the approved brief and specs under `specs/`.
 */
export default function commitMeExtension(_pi: ExtensionAPI) {
  // Intentionally empty during project preparation.
}
